name: CI Pipeline (Tests)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    # Mantenemos Redis para asegurar que no falle la conexión
    services:
      redis:
        image: redis
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install Poetry
      run: |
        curl -sSL https://install.python-poetry.org | python3 -
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Install dependencies
      # GRACIAS al arreglo en pyproject.toml, ahora esto funcionará sin trucos
      run: poetry install

    - name: Run Tests
      env:
        OPENAI_API_KEY: "sk-dummy-key-for-testing"
        MLFLOW_TRACKING_URI: "http://localhost:5000"
        REDIS_HOST: "localhost"
      run: poetry run pytest tests